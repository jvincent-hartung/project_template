# This is a boilerplate .gitignore file for my Python data analysis projects.

# egg info is ignored because it is generated by setuptools and contains irrelevant metadata.
*.egg-info/

# Environment variables are ignored primarily for security reasons.
.env

# I include these as boilerplate since I use pytest and the cache information is not relevant for the repository.
__pycache__/
.pytest)cache/

# These directories are ignored only in the root because I commonly store data in them that doesn't need to be versioned.
# Typically my python projects are for data analysis, so I use /data/ with subdirectories /raw/ and /processsed/ 
# The number of subdirectories can vary, but I usually have a root /results/ directory for outputs.
/data/
/results/